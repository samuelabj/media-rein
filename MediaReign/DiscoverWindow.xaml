<Window x:Class="MediaReign.DiscoverWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:Mr="clr-namespace:MediaReign.Controls"
        Title="DiscoverWindow" Height="374" Width="850">
    <Grid>
		<ListView HorizontalAlignment="Stretch" Name="filesListView" VerticalAlignment="Stretch" VirtualizingStackPanel.IsVirtualizing="False" ItemsSource="{Binding}">
			<ListView.ItemContainerStyle>
				<Style TargetType="ListViewItem">
					<Setter Property="Control.HorizontalContentAlignment" Value="Stretch" />
				</Style>
			</ListView.ItemContainerStyle>
			<ListView.View>
				<GridView>
					<GridViewColumn DisplayMemberBinding="{Binding File}" Header="File" Width="200" />
					<GridViewColumn DisplayMemberBinding="{Binding Match.Name}" Header="Show" Width="200" />
					<GridViewColumn DisplayMemberBinding="{Binding Match.Season}" Header="Season" Width="100" />
					<GridViewColumn DisplayMemberBinding="{Binding Match.Episode}" Header="Episode" Width="100" />
					<GridViewColumn Header="Status" Width="300">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<DockPanel>
									<Mr:AnimatedImage Source="/MediaReign;component/Images/progress_circle.gif" x:Name="progress" HorizontalAlignment="Left" Width="10" Height="10" Visibility="Hidden" />
									<Label Name="status" HorizontalAlignment="Left" VerticalAlignment="Top" />
								</DockPanel>					
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>
				</GridView>
			</ListView.View>
		</ListView>
	</Grid>
</Window>
